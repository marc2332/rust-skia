# Generated by mk-workflows.

name: 'windows-qa'
on:
  push:
    branches-ignore:
      - release
  pull_request:
    branches-ignore:
      - release
  schedule:
    - cron: '0 0 * * *'

concurrency:
  group: 'windows-qa-${{ github.ref }}'
  cancel-in-progress: true

jobs: