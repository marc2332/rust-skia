# Generated by mk-workflows.

name: 'linux-qa'
on:
  push:
    branches-ignore:
      - release
  pull_request:
    branches-ignore:
      - release
  schedule:
    - cron: '0 0 * * *'

concurrency:
  group: 'linux-qa-${{ github.ref }}'
  cancel-in-progress: true

jobs: