# Generated by mk-workflows.

name: 'macos-qa'
on:
  push:
    branches-ignore:
      - release
  pull_request:
    branches-ignore:
      - release
  schedule:
    - cron: '0 0 * * *'

concurrency:
  group: 'macos-qa-${{ github.ref }}'
  cancel-in-progress: true

jobs: