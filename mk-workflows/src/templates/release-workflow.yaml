# Generated by mk-workflows.

name: '$[[workflowName]]'
on:
  workflow_dispatch:
  push:
    branches:
      - release

permissions:
  contents: write

concurrency:
  group: '$[[workflowName]]-${{ github.ref }}'
  cancel-in-progress: true

jobs:
