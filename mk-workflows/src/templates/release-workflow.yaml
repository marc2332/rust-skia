# Generated by mk-workflows.

name: '$[[workflowName]]'
on:
  workflow_dispatch:
  push:
    branches:
      - release

concurrency:
  group: '$[[workflowName]]-${{ github.ref }}'
  cancel-in-progress: true

jobs:
